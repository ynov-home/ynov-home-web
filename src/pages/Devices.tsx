import DeviceManager from "../components/DeviceManager";

const Devices = () => {
    return (
        <div>
            <h1>Liste des Appareils</h1>
            <DeviceManager />
        </div>
    );
};

export default Devices;
